type Props = {
  clientName: string;
  serviceName: string;
  startTime: string;
  endTime: string;
  durationMinutes: number;
  extraLine?: string;
};

export default function AppointmentMiniDetails(props: Props) {
  void props;
  return null;
}
